[build]
  command = "npm install --legacy-peer-deps && next build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"
  NODE_ENV = "production"

[dev]
  command = "next dev"
  port = 3000
