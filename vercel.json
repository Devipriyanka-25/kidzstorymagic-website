{
  "version": 2,
  "buildCommand": "npm install --legacy-peer-deps && npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "public": true,
  "env": {
    "NODE_ENV": "production"
  }
}
